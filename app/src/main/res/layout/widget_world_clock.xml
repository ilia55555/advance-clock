<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/world_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_world_transparent"
    android:orientation="vertical"
    android:padding="6dp">
    <LinearLayout
        android:id="@+id/world_widget_header"
        android:layout_width="match_parent"
        android:layout_height="34dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/world_widget_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="ساعت جهانی"
            android:textColor="#173F3B"
            android:textSize="14sp"
            android:textStyle="bold" />
        <Button
            android:id="@+id/world_widget_previous"
            android:layout_width="42dp"
            android:layout_height="32dp"
            android:background="@drawable/bg_soft_button"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:text="↑"
            android:textColor="#005FA8"
            android:textSize="17sp" />
        <Button
            android:id="@+id/world_widget_next"
            android:layout_width="42dp"
            android:layout_height="32dp"
            android:layout_marginStart="4dp"
            android:background="@drawable/bg_soft_button"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:text="↓"
            android:textColor="#005FA8"
            android:textSize="17sp" />
    </LinearLayout>
    <GridView
        android:id="@+id/world_widget_grid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:horizontalSpacing="4dp"
        android:numColumns="1"
        android:scrollbars="none"
        android:stretchMode="columnWidth"
        android:verticalSpacing="4dp" />
</LinearLayout>
